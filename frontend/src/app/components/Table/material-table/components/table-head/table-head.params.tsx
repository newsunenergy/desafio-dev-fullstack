/* eslint-disable @typescript-eslint/no-unused-vars */
import { ITableHeadParams, ITableHeadProps } from "./table-head.interface";

export const TableHeadParams = (props: ITableHeadProps): ITableHeadParams => {
  // const createSortHandler =
  //   (property: keyof any) => (event: React.MouseEvent<unknown>) => {
  //     props.onRequestSort(event, property as any);
  //   };

  return {
    
  };
};
